spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jeeleducation_lms_dev?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.jeeleducation.lms: DEBUG

